<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.









    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.









        -->
    </style>


    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="EditEventCustomActionButton">
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="Animation" parent="@android:style/Animation.Activity" />

    <style name="Animation.Activity" parent="@style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/def_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/def_open_exit</item>
    </style>

    <style name="ETheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:scrollbarThumbVertical">@drawable/e_scrollbar</item>
        <item name="android:windowAnimationStyle">@style/Animation.Activity</item>
    </style>

    <style name="ETheme.Slide" parent="@style/ETheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="ETheme.Slide.TStatus" parent="@style/ETheme.Slide">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="My.Button" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:textAllCaps">false</item>
    </style>

</resources>